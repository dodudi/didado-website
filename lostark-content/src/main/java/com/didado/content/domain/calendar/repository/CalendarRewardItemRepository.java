package com.didado.content.domain.calendar.repository;

import com.didado.content.domain.calendar.domain.CalendarRewardItem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CalendarRewardItemRepository extends JpaRepository<CalendarRewardItem, Long> {
}
